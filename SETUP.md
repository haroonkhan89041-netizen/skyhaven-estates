# Skyhaven Estates — Setup

1. Extract the ZIP.
2. Open the folder in VS Code.
3. Open Terminal in this folder.
4. Run `npm install`.
5. Run `npm run dev`.
6. For Vercel: import the repository, use build command `npm run build`, output directory `dist`, and keep the Root Directory at the folder containing `package.json`.
